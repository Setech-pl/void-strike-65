FEATURES {
    STARTADDRESS: default = $2000;
}

MEMORY {
    ZP:            start = $0080, size = $0080, type = rw, define = yes;
    MAIN:          start = $2000, size = $2000, type = rw, file = %O, fill = yes, define = yes;
    BOOTTAIL:      start = $4000, size = $1A00, type = ro, file = %O, fill = yes, define = yes;
    STARTAIL:      start = $5A00, size = $1000, type = ro, file = %O, fill = yes, define = yes;
    A2KERNELFILE:  start = $6A00, size = $0100, type = ro, file = %O, fill = yes, define = yes;
    ENTITYFILE:    start = $6B00, size = $0F00, type = ro, file = %O, define = yes;
    BOOT2FILE:     start = $7A00, size = $0800, type = ro, file = %O, define = yes;
    PICKUPFILE:    start = $8200, size = $03F1, type = ro, file = %O, fill = yes, define = yes;
    LIGHTFILE:     start = $8F77, size = $0089, type = ro, file = %O, define = yes;
    BOOT2_RAM:     start = $21C1, size = $0800, type = rw, file = "", define = yes;
    PROJECTILE_RAM:start = $5400, size = $00E4, type = rw, file = "", define = yes;
    STARFIELD_RAM: start = $54E4, size = $092C, type = rw, file = "", define = yes;
    BROADSIDE_RAM: start = $5E10, size = $1A00, type = rw, file = "", define = yes;
    ENTITY_STATE_RAM:start = $8000, size = $0100, type = rw, file = "", define = yes;
    A2KERNEL_RAM:  start = $9000, size = $0100, type = rw, file = "", define = yes;
    ENTITY_CODE_RAM:start = $9000, size = $1000, type = rw, file = "", define = yes;
    PICKUP_CODE_RAM:start = $8776, size = $03F1, type = rw, file = "", define = yes;
}

SEGMENTS {
    ZEROPAGE: load = ZP,   type = zp;
    CODE:     load = MAIN, type = ro;
    RODATA:   load = MAIN, type = ro;
    DATA:     load = MAIN, type = rw;
    PROJECTILES: load = PROJECTILE_RAM, type = bss, define = yes;
    STARFIELD: load = STARTAIL, run = STARFIELD_RAM, type = ro, define = yes;
    BROADSIDE: load = BOOTTAIL, run = BROADSIDE_RAM, type = ro, define = yes;
    A2_KERNEL: load = A2KERNELFILE, run = A2KERNEL_RAM, type = ro, define = yes;
    ENTITY_STATE: load = ENTITY_STATE_RAM, type = bss, define = yes;
    ENTITY_RUN_PAD: load = ENTITY_CODE_RAM, type = bss;
    ENTITY_CODE: load = ENTITYFILE, run = ENTITY_CODE_RAM, type = ro, define = yes,
                 align = $100;
    BOOT_STAGE2: load = BOOT2FILE, run = BOOT2_RAM, type = ro, define = yes;
    LIGHT_RESIDENT: load = PICKUPFILE, run = PICKUP_CODE_RAM, type = ro, define = yes, optional = yes;
    PICKUP_CODE: load = PICKUPFILE, run = PICKUP_CODE_RAM, type = ro, define = yes;
    LIGHT_CODE: load = LIGHTFILE, type = ro, define = yes, optional = yes;
}
