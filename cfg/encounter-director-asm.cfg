MEMORY {
  DIRECTOR_RAM: start = $9D75, size = $028B, type = ro, file = %O, define = yes;
  DIRECTOR_BSS: start = $80F4, size = $000C, type = rw, file = "", define = yes;
}

SEGMENTS {
  DIRECTOR_CODE:   load = DIRECTOR_RAM, type = ro, define = yes;
  DIRECTOR_COMMON: load = DIRECTOR_RAM, type = ro, define = yes;
  LEVEL1_DATA:     load = DIRECTOR_RAM, type = ro, define = yes;
  DIRECTOR_STATE:  load = DIRECTOR_BSS, type = bss, define = yes;
}
